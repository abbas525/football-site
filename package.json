{
  "name": "figma-task",
  "version": "1.0.0",
  "main": "index.js",
 "homepage": "https://abbas525.github.io/football-site",
"scripts": {
  "compile-sass": "sass --watch src/scss/style.scss src/style.css",
  "clean": "rmdir /S /Q build",
  "build": "npm run clean && mkdir build && robocopy src build /E /NFL /NDL /NJH /NJS /NC /NS && rmdir /S /Q build\\scss",
  "deploy": "npm run build && gh-pages -d build"
},

  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.85.0"
  },
  "dependencies": {
    "autoprefixer": "^10.4.20",
    "bootstrap": "^5.3.3",
    "postcss": "^8.5.3",
    "postcss-cli": "^11.0.0",
    "swiper": "^11.2.4"
  }
}
